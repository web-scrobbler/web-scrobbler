var webdriver = require('selenium-webdriver');
var NUM_RETRY = 4;

module.exports.shouldRecogniseATrack = function(driver, url) {

	describe('Connector behaviour', function() {

		it(NUM_RETRY, 'should recognise a playing song', function(done) {

        });

	});
};
