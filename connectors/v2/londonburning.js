'use strict';

/* global Connector */

if (location.hostname === 'londonbu.fastcast4u.com') {

Connector.playerSelector = '#wrapper';

Connector.artistSelector = '#artist-artist';

Connector.trackSelector = '#title-title';

Connector.playButtonSelector = '.play';
}
